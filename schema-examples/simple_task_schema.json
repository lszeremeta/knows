{
    "$schema": "https://raw.githubusercontent.com/lszeremeta/knows/main/knows/schema.json",
    "nodeLabel": "Task",
    "edgeLabel": "dependsOn",
    "nodeProperties": {
        "title": "Sentence",
        "status": {"enum": ["todo", "inProgress", "done"]},
        "estimatedDuration": {"type": "Duration", "min": "PT30M", "max": "P5D"}
    },
    "edgeProperties": {
        "isBlocking": "Boolean"
    }
}
